# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "GitHubActions" ]
  pull_request:
    branches: [ "GitHubActions" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x   
    - name: Restore
      run: dotnet restore GitHubActions/GitHubActions.sln
    - name: Build 
      run: dotnet build GitHubActions/GitHubActions.sln --no-restore
    - name: Test 
      run: dotnet test GitHubActions/GitHubActions.sln --no-build --verbosity normal
